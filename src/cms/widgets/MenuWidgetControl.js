import React from "react";
import MenuWidget from "./MenuWidget";

const MenuWidgetControl = ({ value, onChange }) => (
    <MenuWidget value={value} onChange={onChange} />
);

export default MenuWidgetControl;
